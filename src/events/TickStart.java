package events;
/**
 * This is the class that records whether a tick has started.
 * This will serve as the simulated time for the simulation.
 * The agents will perform their functions only when a TickStart event
 * has taken place and a TickEnd event has not taken place yet.
 * @author lursel
 *
 */
public class TickStart {
public TickStart()
{
	//System.out.println("Tick Start");
}
}
